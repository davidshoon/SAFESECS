# SAFESECS
SAFESECS - Static Analysis For Executables SECurity Scanner

This is a binary security scanner for vulnerable C functions.

It started off as an idea to reverse engineer executables using a C/C++ ELF reader, but then I realise it could be quickly made using bash commands such as objdump.

So here is the POC.
